# Provenance requires a trusted CI environment (GitHub Actions with id-token: write).
# It cannot be produced from a developer machine, so it is opt-in per release:
#   npm run release                      # local publish, no provenance
#   NPM_CONFIG_PROVENANCE=true npm run release   # from CI only
